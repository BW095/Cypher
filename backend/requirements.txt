# Cypher backend — cloud/Bedrock deployment (slim)
# All heavy local-model packages removed — inference is done via Bedrock API.

# ── Web framework ──────────────────────────────────────────────────────
fastapi==0.139.0
uvicorn==0.51.0
starlette==1.3.1
python-multipart
python-dotenv==1.2.2
pydantic==2.13.2
pydantic-settings==2.9.0
pydantic_core==2.33.2

# ── AWS ────────────────────────────────────────────────────────────────
boto3
botocore

# ── Databases ──────────────────────────────────────────────────────────
neo4j==6.2.0
qdrant-client==1.14.3

# ── File watching ──────────────────────────────────────────────────────
watchdog==6.0.0

# ── HTTP ───────────────────────────────────────────────────────────────
httpx==0.28.1
requests==2.32.3
aiohttp==3.12.13

# ── Document processing (lightweight) ──────────────────────────────────
pypdfium2==4.30.1          # PDF text extraction — replaces docling
python-docx==1.2.0         # Word documents
python-pptx==1.0.2         # PowerPoint
openpyxl==3.1.5            # Excel read
xlsxwriter==3.2.9          # Excel write
pandas==2.2.3              # CSV / data tables
beautifulsoup4==4.12.3     # HTML
lxml==5.4.0                # XML / HTML parsing
mail-parser==4.4.0         # Email

# ── Image / vision ─────────────────────────────────────────────────────
pillow==11.2.1             # Image I/O
opencv-python==4.11.0.86   # Image preprocessing

# ── Text processing ────────────────────────────────────────────────────
nltk==3.9.1
tiktoken==0.9.0
regex==2024.11.6

# ── Utilities ──────────────────────────────────────────────────────────
numpy==2.2.6
tqdm==4.67.1
rich==13.9.4
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
packaging==24.2
PyYAML==6.0.2
filelock==3.18.0
fsspec==2025.5.1
diskcache==5.6.3
psutil==7.0.0
tabulate==0.9.0
ujson==5.10.0
chardet==5.2.0
